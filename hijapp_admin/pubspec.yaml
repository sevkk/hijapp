name: hijapp_admin
description: HIJAPP Boutique Admin Panel

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  firebase_storage: ^12.4.4

  # UI
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.1
  image_picker_web: ^4.0.0
  fl_chart: ^1.0.0

  # Utils
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
