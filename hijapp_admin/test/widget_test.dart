// Placeholder. Admin widget tests will be written in Adim 10.
// Firebase.initializeApp gerektigi icin smoke test simdilik atlandi.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('admin placeholder', () {
    expect(1 + 1, 2);
  });
}
